// Import class Animal dari file Animal.ts
import { Animal } from "./Animal";

// Membuat instance baru dari class Animal
const cat = new Animal("cat");

// Memanggil method makeSound dari instance cat
cat.makeSound(); // Output: cat makes a sound
